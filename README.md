# Media Pipe API
simple API for media pipe library

## How to run

1. Install Docker Compose
2. Run it with `docker compose up --build`

---
## How to use
You can check the `localhost:8000/docs` to see and test all endpoints
